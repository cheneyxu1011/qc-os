# QC OS 系统基线与数据同步标准

> 本文件是 QC OS 的产品、流程和数据联动基线。任何功能开发、页面优化、数据库调整、接口对接或视觉改版开始前，必须先完整阅读本文件。
>
> 未经业务负责人明确确认，不得改变本文件中的核心流程、责任规则、报告结构和数据归属。技术实现可以演进，但业务含义不得漂移。

文档优先级：

1. 业务负责人最新明确确认的规则。
2. 本文件 `SYSTEM_RULES.md`。
3. Supabase migration 和正式 API 合同。
4. `docs/integration-plan.md` 等历史规划文档。
5. HTML 原型中的示例文字和演示数据。

低优先级内容与高优先级规则冲突时，必须按高优先级规则处理，并报告差异。

## 1. 系统定位

QC OS 是生产过程中的质量问题提报与改善措施闭环系统，不是通用办公审批系统。

核心目标：

- 记录车缝、压胶、裁剪、业务、QC 等生产相关部门发现的问题。
- 将改善与预防措施分配到具体责任人。
- 通过问题照片、改善照片、执行说明和来源部门复核形成证据闭环。
- 由主管完成归档锁定，并将责任与扣分归集到个人月 KPI。
- 按款号沉淀跨部门质量历史，为 Factory OS 提供质量追溯数据。

正式地址：

- 网站：`https://qc-os.vanwellgroup.com`
- GitHub：`https://github.com/cheneyxu1011/qc-os`
- Vercel 项目：`qc-os`
- Supabase 项目：`vanwell-factory OS`
- S3 bucket：`vanwell-qc-os-media`

## 2. 不得漂移的产品原则

以下规则属于业务基线，除非业务负责人明确修改，否则不得调整：

1. 报告编号按工厂和年份独立递增，由数据库生成，格式为 `{工厂前缀}{YEAR}-{SEQUENCE}`。
2. 一个报告可以选择多个责任部门。
3. 基础信息不设置“报告责任人”；具体责任人统一在部门人员和措施中分配。
4. 部门人员只显示已选责任部门下的人员，并允许按人勾选，不默认通知整个部门。
5. 措施责任人允许跨部门选择，用于主管、QC、业务或其他人员协助改善。
6. 同一条措施有多人负责时，任意一人完成即可把整条措施标记为已执行，同时保留全部被分配人员和实际完成人。
7. 谁提出问题，谁负责复核改善结果。
8. 责任人不能修改原问题照片和问题描述。
9. 复核人不能修改原问题信息、措施内容、责任人的改善照片和执行说明，只能填写复核结果、意见和可选复核证据。
10. 归档后报告主体、责任人和 KPI 均锁定；后续只允许按既定权限补充说明或撤回归档。
11. 投屏显示责任人，但不显示 KPI 扣分。
12. 投屏以问题图片为主，文字为辅助，只显示现场需要处理的问题。
13. 改善报告与归档完整报告必须使用同一份报告数据和同一套表格结构，不得维护两份互相独立的报告内容。
14. 品牌、款号和颜色由 08 款式资料库统一维护，02 改善报告通过选择自动带入，不重复手工输入。
15. 同一品牌和款号允许维护多个颜色；历史报告保存发生时的品牌、款号和颜色快照。
16. 报告人默认同时作为复核人；复核人可从人员库全部人员中改选。
17. 审批人默认取“厂长”部门人员，并允许业务员从人员库全部人员中按所在工厂改选。
18. 草稿按报告人隔离显示，不占用正式报告编号，不生成措施任务，不进入复核、归档或 KPI；只有正式“保存报告”才进入闭环。
19. “仅通知”是正式措施类型，责任人只需确认已知，不强制上传改善照片，不进入来源部门效果复核。

## 3. 固定页面结构

桌面端左侧导航固定为：

| 编号 | 页面 | 主要职责 |
| --- | --- | --- |
| 01 | 现场双屏 | 按接收部门和款号展示未关闭问题，以问题图片为主 |
| 02 | 改善报告 | 创建问题、上传问题照片、填写原因和措施、生成录入阶段报告 |
| 03 | 我的任务 | 责任人查看措施、查看原问题、上传改善照片、填写执行说明并完成任务 |
| 04 | 部门复核 | 问题来源部门查看前后证据、填写复核意见并通过或退回 |
| 05 | 归档审批 | 展示完整正式报告，主管检查资料、归档并锁定 KPI |
| 06 | 月 KPI 汇总 | 按个人、部门、月份和款号汇总归档后的扣分 |
| 07 | 人员库 | 测试阶段维护部门和人员，未来由 People OS 同步 |
| 08 | 款式资料库 | 测试阶段维护品牌、款号和颜色组合，未来由 Factory OS 同步 |

移动端固定为现场动作入口：

- 拍照提报
- 我的任务
- 待复核
- 归档

移动端与桌面端必须读取和写入同一份后端数据，不得建立独立移动端台账。

## 4. 固定闭环流程

业务闭环固定为五个阶段：

| 阶段 | 报告状态 | 完成条件 | 下一步 |
| --- | --- | --- | --- |
| 1. 问题提交 | `submitted` | 基础信息、问题描述、问题照片、原因、至少一条措施已保存 | 责任人执行 |
| 2. 措施执行 | `executing` | 措施进入责任人的任务列表 | 等待全部措施完成 |
| 3. 待复核 | `pending_review` | 每条改善措施已有照片和执行说明，且每条“仅通知”已确认 | 来源部门复核 |
| 4. 待归档 | `pending_archive` | 来源部门复核通过 | 主管归档 |
| 5. 已归档 | `archived` | 主管确认归档，报告和 KPI 锁定 | 历史查询与 KPI 汇总 |

复核不通过时使用 `review_rejected`，退回责任人继续改善，不得直接关闭报告。

报告关闭的唯一正常条件是：

`责任人完成措施 -> 问题来源部门复核通过 -> 主管归档`

## 5. 报告字段标准

### 5.1 基础信息

- 报告编号：系统生成，不允许手工改变顺序。
- 所属工厂：从工厂资料库选择，并决定报告编号前缀和独立流水。
- 发现日期
- 品牌，由 08 款式资料库自动带入
- 款号，从 08 款式资料库选择
- 颜色，只显示所选品牌和款号已维护的颜色
- 严重程度
- 问题来源部门
- 责任部门，可多选
- 部门人员，可多选且只显示已选责任部门人员
- 是否计入 KPI
- 报告人，测试阶段从全部人员中选择

### 5.2 问题信息

- 问题描述
- 问题照片，可多张
- 根本原因分析

### 5.3 改善与预防措施

每条措施必须独立保存：

- 序号
- 措施类型：临时改善、永久改善、预防措施、仅通知
- 措施内容
- 责任人，可多选和跨部门
- 计划完成日期
- 提醒说明
- 状态

措施支持新增和删除。删除措施必须同时处理其责任人分配、改善附件和审计关系，不得留下孤立数据。

“仅通知”仍需指定接收人并在“我的任务”中确认已知，但不要求改善照片，确认说明可选，也不进入来源部门效果复核证据区。未确认前仍属于未完成任务。

### 5.4 效果验证与确认

效果验证不是创建报告时由报告人代填，而是在闭环过程中产生：

- 对应措施序号，每个序号独立分行显示，不得把多条措施的证据合并成一段
- 改善照片，原则上每条措施至少一张
- 实际完成人
- 完成日期
- 执行说明
- 来源部门复核结果
- 复核人
- 复核日期
- 复核意见
- 可选复核照片
- 报告人默认带入复核人，必要时可从完整人员库改选
- 审批人默认带入所在厂长，归档前可从完整人员库改选

## 6. 正式报告显示标准

02 改善报告底部的报告和 05 归档审批中的报告共享同一份数据结构。

固定章节：

1. 一、问题描述
2. 二、根本原因分析
3. 三、改善与预防措施
4. 四、效果验证与确认
5. 五、备注

显示规则：

- 02 录入阶段保留完整基础信息和固定章节。
- 02 的“四、效果验证与确认”允许显示，但内容必须为空白或明确标注“待责任人执行后填写”。
- 02 不得伪造改善照片、完成日期、复核意见或已执行状态。
- 05 使用同一报告主体，并补入真实的改善照片、完成日期、执行说明和复核意见。
- 导出 PDF、图片和打印都必须基于同一份完整报告数据。

## 7. 唯一数据来源与快照规则

### 7.1 当前测试阶段

| 数据 | 当前唯一来源 |
| --- | --- |
| 报告、责任部门、部门人员、措施、任务、复核、归档、KPI、评论、审计 | Supabase `qc_*` 表 |
| 原问题、改善、复核、归档图片或 PDF 文件 | Amazon S3 |
| 图片 metadata、S3 bucket 和 key | Supabase `qc_attachments` |
| 部门和人员 | Supabase `qc_departments`、`qc_people`、`qc_person_departments` |
| 工厂名称、代码和报告前缀 | Supabase `qc_factories` |
| 品牌、款号和颜色 | Supabase `qc_style_catalog` |
| 页面演示数据 | 仅用于尚未接后端的界面预览，不得视为正式数据 |

浏览器内存、HTML 常量、URL hash 和 DOM 内容都不是正式数据源。

### 7.2 Future: Factory OS

未来由 Factory OS 提供：

- 品牌
- 款号
- 颜色
- 订单
- 生产批次
- 工序和车间
- 生产状态

QC OS 保留稳定外键：

- `factory_style_id`
- `factory_order_id`
- `factory_production_batch_id`

同时保留报告发生时的品牌、款号、颜色快照，保证历史报告不受 Factory OS 后续修改影响。

Factory OS 接入前：

- 08 款式资料库是品牌、款号和颜色的测试阶段主数据。
- 一条款式资料代表一个“品牌 + 款号 + 颜色”组合。
- 同一品牌和款号可以有多个颜色。
- 修改款式资料不得改写已创建报告的品牌、款号和颜色快照。
- 删除已使用资料时采用停用，不物理删除历史关联。

### 7.3 Future: People OS

未来由 People OS 提供：

- 部门
- 人员
- 人员所属部门
- 角色
- 在职状态
- 登录身份与权限

QC OS 必须同时保存：

- 稳定 ID，用于系统关联。
- 姓名和部门名称快照，用于历史报告显示。

人员改名、转部门或离职后，不得改写已归档报告中的历史姓名和部门。

People OS 接入前：

- 不开发重复的正式登录、角色和权限体系。
- 人员库继续作为测试阶段手动主数据。
- 对接预留代码和字段不得删除。

## 8. Supabase 表关系标准

数据库迁移文件是结构的权威来源：

`supabase/migrations/20260727155030_create_qc_os_pilot_schema.sql`

核心关系：

```text
qc_reports
  -> qc_factories
  -> qc_style_catalog
  -> qc_report_responsible_departments
  -> qc_report_responsible_people
  -> qc_corrective_actions
       -> qc_action_assignees
       -> qc_attachments (action_after)
  -> qc_attachments (problem_before/review_evidence/archive_*)
  -> qc_reviews
  -> qc_archives
  -> qc_kpi_entries
  -> qc_comments
  -> qc_audit_events
qc_report_drafts
  -> qc_people (报告人)
  -> qc_factories
  -> qc_style_catalog
```

数据写入原则：

- 报告是闭环聚合根，其他业务记录必须关联 `report_id`。
- 每份报告保存 `factory_id` 以及工厂名称、代码、编号前缀快照。
- 工厂编号规则固定为：新长江 `XCJ`、如东万誉 `RD`、万航芜湖 `WH`、越南古芝 `CC`、越南隆安 `LA`、掘港 `JG`。
- 编号序列按 `factory_id + report_year` 独立计算，不允许跨工厂共用流水。
- 措施执行资料必须关联 `action_id`。
- 删除未归档报告时，子数据按外键规则清理。
- 已归档报告不得物理删除。
- 所有关键动作写入 `qc_audit_events`。
- 草稿主体保存在 `qc_report_drafts.draft_data`，按报告人查询；正式提交成功后删除对应草稿。
- 前端不得直接使用 Supabase secret key。
- 服务端通过 `SUPABASE_SECRET_KEY` 访问 QC 表。

## 9. 数据同步矩阵

同一字段在不同页面出现时，必须来自下列同一数据，不允许页面各自保存副本：

| 数据 | 02 改善报告 | 03 我的任务 | 04 部门复核 | 05 归档审批 | 01 投屏 | 06 KPI |
| --- | --- | --- | --- | --- | --- | --- |
| 报告编号、款号、品牌、颜色 | 编辑/显示 | 显示 | 显示 | 显示 | 显示 | 关联 |
| 所属工厂 | 选择/显示 | 显示 | 显示 | 显示/筛选 | 显示/筛选 | 汇总筛选 |
| 问题描述 | 编辑 | 只读 | 只读 | 只读 | 摘要 | 关联 |
| 问题照片 | 上传 | 只读 | 只读 | 只读 | 主图 | 不显示 |
| 责任部门和部门人员 | 选择 | 显示 | 显示 | 显示 | 责任提示 | 归集 |
| 措施内容和计划日期 | 编辑 | 只读 | 只读 | 只读 | 状态摘要 | 逾期依据 |
| 措施责任人 | 选择 | 任务过滤 | 显示 | 显示 | 显示 | 追加扣分归集 |
| 改善照片和执行说明 | 空白 | 上传/填写 | 只读 | 只读 | 可显示进度 | 完成依据 |
| 复核意见 | 空白 | 不可编辑 | 填写 | 只读 | 状态 | 关闭依据 |
| 归档结果 | 空白 | 状态 | 状态 | 审批/锁定 | 关闭后隐藏 | 锁定 |

任何字段修改后，所有相关页面刷新时必须读到同一值。

## 10. 图片同步标准

S3 保持 private，Supabase 不存图片二进制。

固定附件类型：

- `problem_before`
- `action_after`
- `review_evidence`
- `archive_pdf`
- `archive_image`
- `comment_attachment`

固定 S3 key 结构：

```text
qc-os/{year}/{report_no}/problem-before/{file}
qc-os/{year}/{report_no}/action-after/action-{sequence_no}-{file}
qc-os/{year}/{report_no}/review-evidence/{file}
qc-os/{year}/{report_no}/archive/{file}
```

同步规则：

1. 前端向服务端申请 presigned upload。
2. 浏览器将文件直接上传 S3。
3. 报告或任务保存时，将 S3 key 和 metadata 写入 `qc_attachments`。
4. 页面读取附件记录后，通过短期签名查看链接显示私有图片。
5. 问题描述、任务、复核、归档报告和投屏必须引用同一条 `problem_before` 记录。
6. S3 上传成功但数据库保存失败时，应清理孤立对象或进入可追踪的待清理队列。
7. 删除未归档附件时，同时删除数据库索引和 S3 对象。

## 11. 现场投屏标准

投屏不是后台导航页，而是独立的现场问题看板。

固定规则：

- 不显示桌面端七个导航。
- 不显示 KPI 扣分或“投屏不显示扣分”等解释文字。
- 问题图片占主要视觉面积。
- 显示问题摘要、款号、报告编号、责任人、期限、状态和严重程度。
- 逾期自动红色提醒。
- 支持按款号筛选。
- 支持按工厂筛选。
- 默认只显示未关闭问题。
- 部门分组必须动态生成，不把车缝、压胶、裁剪或业务写死为永久固定数量。
- 每个部门显示“其他部门反馈给本部门、需要本部门改善”的问题。
- 新增部门后，不应通过手工复制一个新看板页面实现。

## 12. KPI 标准

KPI 采用每人每月 100 分倒扣。

当前业务规则：

- 一般问题：扣 1 分
- 严重问题：扣 3 分
- 不可接受或客户投诉：扣 5 分
- 每项措施逾期：追加扣 1 分
- 重复发生：追加扣 2 分

归集规则：

- 报告基础扣分按报告中的部门人员归集。
- 逾期和未执行按措施责任人追加扣分。
- KPI 只有归档后才能正式计算和锁定。
- 投屏不显示扣分。
- 已锁定 KPI 的来源记录必须可追溯到报告和措施。

## 13. 当前实现概览

截至 2026-07-28：

### 当前生产数据快照

- 有效部门：6 个
- 有效人员：8 人
- 正式报告：0 条
- 措施：0 条
- 附件索引：0 条
- 复核：0 条
- 归档：0 条
- KPI 明细：0 条

该快照用于说明正式试运行的干净起点，不是需要长期手工维护的统计值。实时数量以后以 Supabase 查询结果为准。

### 已接真实后端

- GitHub、Vercel、正式域名已连接。
- Supabase QC 表和测试人员数据已建立。
- S3 private bucket、IAM 上传用户和 CORS 已配置。
- `/api/uploads/presign` 可生成 S3 presigned POST。
- 02 改善报告的问题图片可上传 S3。
- `/api/qc/reports` 可保存报告、责任部门、部门人员、措施、措施责任人、图片索引和提交审计。
- `GET /api/qc/reports` 可按状态、款号和责任部门读取报告列表。
- `GET /api/qc/reports/{report_no}` 可读取同一报告的完整闭环数据。
- 报告读取接口可为 S3 私有附件生成 5 分钟有效的短期查看链接。
- 08 款式资料库通过 Supabase `qc_style_catalog` 统一维护品牌、款号和颜色组合。
- 08 款式资料库支持新增、编辑和停用；桌面端 02 与移动端提报均读取同一资料库。
- 新报告保存时由服务端按 `style_catalog_id` 写入品牌、款号和颜色快照，不能信任浏览器自报文本。
- 新报告保存时同时记录默认复核人和预设审批人，供 04 部门复核和 05 归档审批继续使用。
- 生产环境已通过包含图片的保存冒烟测试。
- 工厂资料由 `qc_factories` 统一维护，报告编号按工厂和年份独立生成。
- 历史改善记录读取真实报告数据；未归档报告可删除，已归档报告禁止物理删除。
- 改善历史支持按月份、工厂和款号导出 Excel。
- 01 现场投屏读取真实未关闭报告，按责任部门动态分组，并使用真实问题图片。
- 03 我的任务读取真实报告和措施责任人；人员筛选、问题图片、问题描述、措施内容和统计数据均来自保存数据。
- 03 我的任务的改善照片按任务所属报告编号上传 S3，执行说明、实际完成人和完成状态写回同一措施。
- 04 部门复核支持上传多张可选复核证据照片，复核意见、复核结果和附件索引写入 Supabase。
- 05 主管可将待归档报告退回 04 补充复核资料，保留全部问题、措施、执行和既有复核证据，并记录审计事件。

### 仍为原型或演示数据

- 05 归档报告的撤回归档权限和归档后补充评论。
- 06 月 KPI 实际计算与查询。
- 移动端大部分业务保存动作。

页面可见不等于功能已接数据库。后续验收必须以刷新页面后仍能从 Supabase/S3 还原数据为准。

## 14. 暂缓范围

以下内容明确延后，不应在当前阶段提前重构：

- People OS 登录和账户关联。
- 正式角色权限体系。
- People OS 人员自动同步。
- Factory OS 款号和生产批次实时同步。

允许保留接口、环境变量和字段预留，但不要建设第二套临时正式系统。

## 15. 后续开发顺序

在不改变业务规则的前提下，下一阶段顺序固定为：

1. 报告详情和列表读取 API。
2. S3 私有图片短期查看链接。
3. 03 我的任务真实数据、改善附件、执行说明和任意一人完成规则。
4. 04 来源部门复核真实数据、通过和退回。
5. 05 完整报告、主管归档和锁定。
6. 01 动态部门投屏和款号筛选。
7. 06 月 KPI 计算和历史查询。
8. 07 人员库 Supabase 同步。
9. 08 款式资料库和 02 基础信息联动。
10. 移动端接入以上同一 API。
11. Factory OS 和 People OS 正式对接。

## 16. 变更前强制检查

每次开发前必须：

1. 阅读本文件。
2. 检查当前 Git 状态，不覆盖用户未提交的修改。
3. 明确本次改动影响的数据表、API 和页面。
4. 确认是否改变核心业务规则；若改变，必须先得到业务负责人明确确认。
5. 优先复用现有字段、状态和报告结构。
6. 数据库变化必须新增 Supabase migration。
7. 不得把正式数据重新放回 HTML 常量或浏览器本地状态。

每次开发后至少验证：

- 桌面端刷新后数据仍存在。
- 移动端和桌面端读取同一数据。
- 02 的品牌、款号和颜色来自 08 款式资料库，并保存正确的报告快照。
- 问题照片在报告、任务、复核、归档和投屏引用一致。
- 措施责任人与任务列表一致。
- 任意一名共同责任人完成后，整条措施状态正确。
- 来源部门才是业务复核方。
- 02 报告不提前出现伪造的效果验证结果。
- 05 完整报告包含真实效果验证。
- 归档后不能继续修改锁定字段。
- 测试数据和 S3 测试对象在验证后清理。

## 17. 修改本基线的规则

只有以下情况可以修改本文件中的业务规则：

- 业务负责人明确提出新规则。
- Factory OS 或 People OS 的正式接口合同要求调整数据边界。
- 已确认的法规、客户要求或公司制度发生变化。

纯技术重构、视觉优化、组件拆分、框架升级或开发便利，不构成改变业务规则的理由。

如果业务规则确实改变，必须在同一个 Git 提交中同时更新：

- 本文件
- 相关数据库 migration
- 相关 API 或页面
- 对应测试说明
