# QC OS Development Guardrail

Before analyzing, editing, testing, or deploying this repository, read `SYSTEM_RULES.md` in full.

Treat `SYSTEM_RULES.md` as the product and data synchronization baseline. Do not change its core workflow, ownership rules, report structure, data sources, or synchronization rules without explicit confirmation from the business owner.

When implementation and `SYSTEM_RULES.md` differ, report the mismatch. Do not silently redefine the intended behavior to match the prototype.

